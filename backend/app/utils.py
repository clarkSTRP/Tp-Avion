# ce fichier a pour but d'héberger des fonctions utilitaires

#voir pour reduire la duplication de code dans les endpoints
# def rows_to_dicts(result):
#     return [dict(r) for r in result.mappings()]